import React from 'react';

export const VezzitechLogo = ({ className = "" }: { className?: string }) => (
  <span className={`font-semibold tracking-tight text-white font-sans italic ${className}`}>
    Vezzitech
  </span>
);
